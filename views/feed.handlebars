<div class="mt-1 fixed flex justify-center items-center w-5/6 h-screen filter drop-shadow-lg">
    <div class="rounded-lg bg-dark h-full w-4/6 overflow-auto">
        {{#each posts}}
            <div class="post">
                {{> post this}}
            </div>
        {{else}}
            <p class="text-center text-white">No posts to display.</p>
        {{/each}}
    </div>
</div>